using UnityEngine;
using UnityEngine.Networking;
using System.Collections;

public class testRpc : MonoBehaviour
{
    NetworkView networkView;

    void Start()
    {
        //networkView = new NetworkView();
        //networkView.RPC("PrintText", RPCMode.All, "Hotdog");
    }
}